import {argv} from 'node:process';
import path from 'node:path';

// Function to obtain the arguments passed
const getArguments = () => {
  const args = argv;
  return args;
}

// Function to check if it is an absolute or a relative path
console.log(getArguments());




